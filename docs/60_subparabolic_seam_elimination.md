# The subparabolic seam is not an independent recurrence owner

Status: **RIGOROUS OWNER REDUCTION / OPEN GLOBAL ASSEMBLY**.  This note combines the PDE-derived record selector, radial high-tail memory erosion, and subcritical-tail self-absorption.  It does not claim global regularity.

## 1. What `S` used to mean

The minimal graph previously retained

\[
S=\text{subparabolic critical-shell renewal}
\]

because the unique future-heat currency becomes cheap when

\[
a=2\nu(T-t)N^2\ll1.
\]

A cheap heat-coordinate jump does not by itself contradict repeated critical renewal.  The missing question was whether a critical shell could keep appearing below the terminal heat scale without paying a physical owner.

The answer is now no: it cannot be **silent**.  Two distinct situations exhaust the possibilities.

## 2. A higher critical crossing is automatically a fresh radial-work event

Use the highest-active-shell selector of Theorem BJ with activation level `A_*`.  At a first higher critical crossing at shell scale `N`, continuity gives

\[
N E_N(t)\ge A_*^2
\]

(up to the fixed hard-shell normalization already built into the selector).

Apply Theorem BK with `eta=A_*^2`.  For `R=rho N`, within

\[
L_N
=
O\!\left(\frac{\log N}{\nu N^2}\right)
\]

before the crossing one has

\[
\boxed{
\int_{t-L_N}^{t}
e^{-2\nu R^2(t-s)}
\Phi_\uparrow(R,s)\,ds
\ge
\frac{A_*^2}{2N}.
}
\]

Thus a higher critical crossing is not observer motion and is not a free Boolean transition.  It is funded by actual positive nonlinear energy crossing a radial Fourier boundary.

If such crossing scales tend to infinity along times tending to a candidate singular time `T`, then `L_N->0`, so the funding events themselves occur arbitrarily close to `T`.

## 3. No higher crossing means the UV tail must be externally serviced

Suppose instead that on a time interval the highest active shell never rises above a fixed `Q`.  Every strictly higher shell then remains below the chosen critical activation level.

The low-frequency `H^1` content below `Q` is bounded by `C Q^2E_*`.  Hence if the full enstrophy reaches arbitrarily large new levels without a higher crossing, the higher tail must also reach arbitrarily large new levels.

At the first hitting time of each such tail level,

\[
\frac d{dt}\|\nabla P_{>Q}u\|_2^2\ge0.
\]

Theorem BL then gives

\[
\boxed{
\mathcal W_{ext,Q}
\ge
\frac{3\nu}{4}
\|\Delta P_{>Q}u\|_2^2.
}
\]

So a critical-subthreshold UV tail cannot carry singular record growth by autonomous high--high self-interaction.  A lower-frequency incidence/boundary owner must be active.

## 4. Physical handoff of the fresh/external work

After Sections 2--3 there is no independent `S` energy mechanism left.  Every subparabolic renewal has become actual work and must be typed by its real interaction geometry.

### Comparable/local radial crossing

This is actual positive HH/edge work.  Current Wang canonical routing already keeps geometry/phase-bad positive work as terminal transfer loss and passes only geometry-good work to the signed-cell Young/registration layer.  Signed-good generated epochs have their own finite backward parabolic depth.

### Nonlocal radial crossing

Theorem BI forces a comparable high-frequency companion at the same closed triad.  Old low-strain companion service decays by Theorem BB; indefinite renewal therefore enters the `U` high-companion ancestry seam.

### Resolved low--high incidence

Current Wang smooth `Q^2` energy law and resolved-interface quotient split the physical incidence only after common observer transport is removed:

- conservative skew donor/relink is same-event provenance with zero recursive depth;
- symmetric resolved work is the existing strain/deformation owner.

### Material/source/interface change

These remain explicitly typed `R`/source owners.  They are not hidden inside the radial flux scalar.

## 5. Updated minimal graph

The recurrence graph therefore contracts from

\[
\boxed{S/U/R}
\]

to

\[
\boxed{U/R}
\]

**at the owner-reduction level**.

This does not mean `U` and `R` are solved.  It means only that subparabolicity itself is no longer an independent escape mechanism.  A subparabolic critical record must continuously disclose one of the already-visible physical input owners above.

The remaining programme-level tasks are therefore:

1. exhaust repeated high-companion/new-ancestry funding under the actual positive radial/edge-work law (`U`);
2. prove physical state descent / hysteretic material-current reselection so relink/fragmentation cannot clone ancestry (`R`);
3. assemble those owner laws on one measurable candidate singular path without double charging.

## 6. Why this is stronger than an own-scale lifespan lemma

The earlier proposed closure of `S` asked for a forward full-PDE lifespan of order `N^-2`.  That would be sufficient, but it is stronger than necessary.

The NSE itself gives a more physical statement:

- viscosity erases old high-tail memory at rate `2nu R^2`;
- a critical shell therefore certifies recent inward radial funding;
- a subcritical higher tail cannot self-fund record growth against viscosity.

So one need not infer future continuation before identifying the cause.  The actual PDE current already exposes the cause on the existing smooth interval.

No recurrence termination or global-regularity theorem is claimed here.
