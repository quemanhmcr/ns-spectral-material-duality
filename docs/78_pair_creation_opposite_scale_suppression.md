# Heterochiral pair creation is quadratically suppressed by the opposite-helicity recipient scale

Status: **EXACT TRIANGLE GEOMETRY / PHYSICAL EDGE-CAPACITY CONSEQUENCE**.

## 1. Normal form

Use the heterochiral split normal form

\[
-b<a<c,
\qquad a,b,c>0,
\]

with donor `a`, high same-helicity recipient `c`, and opposite-helicity recipient of physical radius `b`.

With the common closed-triad factor `R_triangle`, the donor work is

\[
Q=(b+c)|R_\triangle|,
\]

and the opposite recipient fraction is

\[
p_o=\frac{c-a}{b+c}.
\]

Therefore the pair-creation charge is exactly

\[
\boxed{
\mathcal P_\triangle
=Qp_ob
=b(c-a)|R_\triangle|.
}
\]

## 2. Triangle geometry gives a second low-scale factor

The three physical radii form a strict triangle, hence

\[
c<a+b.
\]

Therefore

\[
0<c-a<b
\]

and

\[
\boxed{
\mathcal P_\triangle<b^2|R_\triangle|.
}
\]

So if the opposite-helicity recipient is the low leg of a separated interaction, pair creation carries two powers of that low physical scale.  The apparently large high-frequency separation cannot remove this low-scale suppression.

## 3. Direct physical-edge capacity form

The positive energy work received by the opposite-helicity mode is

\[
T_o^+=(c-a)|R_\triangle|,
\]

so

\[
\boxed{\mathcal P_\triangle=bT_o^+.}
\]

Current Wang physical edge registration gives the pointwise native capacity bound for a child of radius `b`,

\[
T_o^+\le A_o=4b|a_0a_1a_2|.
\]

Hence

\[
\boxed{
\mathcal P_\triangle
\le4b^2|a_0a_1a_2|.
}
\]

This is a physical modal-capacity statement, not a causal replacement of actual pair work by capacity.

## 4. Owner placement

- If the opposite-helicity recipient is low, pair creation is quadratically low-scale suppressed and belongs in the separated low/high analysis.
- If all three modes are comparable, Theorem CA instead gives the scale-equivalence `P ~ V/K`.
- If the low leg is the donor rather than the opposite recipient, the present low-recipient suppression is not invoked; that geometry is the genuine low-donor/high-pair backreaction branch.

Thus the separated heterochiral branch itself has two physically distinct subgeometries.  They must not be merged into one generic UV estimate.

No global finiteness of `P_create` is claimed here.
