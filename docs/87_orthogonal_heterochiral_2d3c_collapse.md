# The orthogonal equal-scale heterochiral Hadamard seed collapses exactly to a 2D3C passive-scalar class

Status: **EXACT NSE/POLARIZATION IDENTITY / COUNTEREXAMPLE TO STATIC-CHANNEL CASCADE INFERENCE**.

## 1. Coordinate-free seed

Let `e_p,e_q` be orthonormal vectors and

\[
n=e_p\times e_q.
\]

Take equal-length planar wavevectors

\[
p=Ne_p,
\qquad
q=Ne_q,
\]

with helicities `+` on `p` and `-` on `q`.  In the triad-normal gauge,

\[
h_+(p)=\frac{e_q+i n}{\sqrt2},
\qquad
h_-(q)=\frac{-e_p-i n}{\sqrt2}.
\]

Arbitrary complex scalar amplitudes multiply these vectors; the conclusions below are unchanged.

## 2. The first Hadamard children are forced to be linearly polarized

For `m=p+q`, the unordered advective source contains

\[
(q\cdot h_+(p))h_-(q)
+(p\cdot h_-(q))h_+(p).
\]

Since

\[
q\cdot h_+(p)=\frac N{\sqrt2},
\qquad
p\cdot h_-(q)=-\frac N{\sqrt2},
\]

the bracket is

\[
-\frac N2(e_p+e_q)-iNn.
\]

The first term is parallel to `m` and is killed by Leray.  Thus

\[
\boxed{\widehat{\mathcal N}(m)\parallel n.}
\]

Using the reality partner `-q` gives the sibling `l=p-q` and the same conclusion:

\[
\boxed{\widehat{\mathcal N}(l)\parallel n.}
\]

A vector parallel to `n` has equal-magnitude projections on the `+` and `-` helical fibers of any planar wavevector.  Therefore the PDE does **not** select the single-helicity children assumed by the static channel ladder; it births both helicities with equal magnitude and a fixed linear polarization.

## 3. The two first-generation siblings cannot drive the next Hadamard edge

Both sibling wavevectors lie in the `e_p,e_q` plane while both generated velocity vectors are parallel to `n`.  Hence

\[
(p-q)\cdot \widehat u(p+q)=0,
\qquad
(p+q)\cdot \widehat u(p-q)=0.
\]

The unordered quadratic interaction of these two generated siblings is therefore exactly zero:

\[
\boxed{
\mathcal B(\widehat u_{p+q},\widehat u_{p-q})
+
\mathcal B(\widehat u_{p-q},\widehat u_{p+q})=0.
}
\]

So the static `p+q` / `p-q` channel composition does not automatically produce `2p` in the actual vector NSE.

## 4. Full physical state is 2D3C

All wavevectors lie in the plane spanned by `e_p,e_q`, so write

\[
u=v+w n,
\qquad
v\cdot n=0.
\]

The horizontal field `v` is a two-dimensional incompressible flow.  Its two initial Fourier modes have the same Laplacian eigenvalue `N^2`.  Hence its streamfunction `psi` satisfies

\[
-\Delta\psi=N^2\psi
\]

on the whole horizontal initial field.  Therefore

\[
J(\psi,\Delta\psi)=0,
\]

so the horizontal nonlinear vorticity equation has zero Euler term.  Exactly,

\[
\boxed{v(t)=e^{-\nu N^2t}v(0).}
\]

The vertical component obeys the passive advection--diffusion equation

\[
\boxed{
\partial_t w+v\cdot\nabla w=\nu\Delta w.
}
\]

It has no feedback into `v` because `partial_n=0`.  Thus this entire Hadamard seed belongs to a globally regular 2D3C class.

## 5. Meaning of the failed Action adversary

Action `31651133703` found strong first-generation birth in both desired and opposite helical fibers, but the proposed second Hadamard modes stayed at about `1e-20`.  That failure is consistent with the exact polarization/2D3C mechanism above and correctly rejects the stronger static-ladder inference.

The lesson is structural:

> single-helicity closed-triad channels cannot be composed across generations without carrying the **full vector polarization state** generated by the PDE.

This is precisely the kind of state information lost by a root-only energy/helicity genealogy.

**Classification: EXACT NSE/PDE IDENTITY + ACTION CONSISTENCY.**
