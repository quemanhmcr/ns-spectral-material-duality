# The global enstrophy source graph has one positive nonlinear owner: signed-frequency split variance

Status: **EXACT GLOBAL OWNER QUOTIENT / OPEN RATE-CONTROL PROBLEM**.  This theorem changes the placement of several earlier mechanisms in the global proof graph; it does not declare their auxiliary local theorems unnecessary.

## 1. Start from the unforced full-state PDE, not a selected representation

Theorem BR gives on the full physical Fourier--helical state

\[
\boxed{
\frac12Y'+\nu Z
=\mathcal V_{split}-\mathcal V_{merge},
}
\]

where

\[
\mathcal V_{split}\ge0,
\qquad
\mathcal V_{merge}\ge0.
\]

Both are pushforwards of the canonical closed-triad work law.  No smooth carrier, material current, first-bad selector, hard product cell, or reduced ancestry state appears in their definition.

## 2. Positive and negative global owners are now rigid

The exact signs are:

- `1 donor -> 2 recipients`: `+Q Var(x)` -- the **only positive nonlinear enstrophy owner**;
- `2 donors -> 1 recipient`: `-Q Var(x)` -- nonlinear enstrophy destruction;
- viscosity: `-nu Z` -- physical killing;
- zero-work/affine signed-frequency transport: zero second-moment production.

Therefore a global record satisfies

\[
\boxed{\mathcal V_{split}\ge\nu Z+\mathcal V_{merge}.}
\]

Any decomposition into HH, resolved incidence, strain, relink, phase, radial crossing, material service, or interface work is a **refinement of the rate and provenance of the same closed-triad split/merge law**.  Such a refinement may be essential to bound `V_split`; it is not an additional global source term.

## 3. Phase, strain and radial geometry keep their correct jobs

This quotient does not erase the earlier physics.

- Interaction phase controls the sign and size of the common triad factor `R_triangle`; it determines whether a split/merge is active and at what rate.
- Strain/relink calculus identifies how a low--high piece of that rate is carried after common observer motion is removed.
- Radial gates identify where the split/merge law transports energy relative to a spectral control surface.
- Cauchy/Kelvin covariance describes state deformation and current sampling, not an extra energy source.

The new statement is only that these are **typed coordinates of the unique nonlinear triad law**, not independent additive enstrophy currencies.

## 4. Material/reduced-state resolution is an admissibility seam, not an enstrophy owner

A reduced Kelvin state may still fail kernel purity; a material selector may still require a state-map/clock face; a finite current shape may still require a nontruncated state.  Those are real mathematical seams whenever an auxiliary theorem is expressed in such variables.

But failure of state descent cannot create a new term in

\[
\mathcal V_{split}-\mathcal V_{merge}.
\]

Hence `R` should no longer be drawn next to split variance as though both could feed global enstrophy.  Its correct placement is:

\[
\boxed{
R=\text{admissibility/state-fidelity condition on auxiliary localization.}
}
\]

If an `R` condition fails, the auxiliary argument stops or acquires a resolution face; the full-state modal PDE ledger remains unchanged.

## 5. Updated main physical graph

The global physical recurrence problem therefore contracts to

\[
\boxed{
\text{split-variance production}
\quad\text{vs}\quad
\text{merge destruction + viscous killing}.
}
\]

The unresolved question is now rate control/non-explosion of `V_split`, with radial/local/UV/strain/material theorems available as refinements when needed.

This is a major owner quotient, not a recurrence-termination theorem and not a global-regularity claim.
