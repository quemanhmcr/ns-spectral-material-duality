# Final dense heterochiral source/ancestry attachment frontier after the positive-net critical-action reduction

Status: **OPEN BRIDGE / PROOF-FRONTIER COMPRESSION**.  This note records what is actually left after Theorems BY--CV.  It makes no new regularity claim.

## 1. The global singularity requirement is now one signed physical action

Let

\[
J_1(t)
=\int_0^\infty
[\Phi_\uparrow(R,t)-\Phi_\downarrow(R,t)]\,dR.
\]

CF proves:

\[
\boxed{
T<\infty\text{ first singular}
\Longrightarrow
\int_0^T[J_1(t)]_+dt=\infty.}
\]

Equivalently, the positive net heterochiral critical-pair source

\[
N=P_{create}-P_{ann}=J_1/2
\]

must have infinite positive action.  Gross create/annihilate cycling is already quotiented before the criterion is read.

No other global nonlinear energy/enstrophy owner remains independent of this current.

## 2. Deep/nonlocal geometry is no longer an independent escape

CI gives exact Waleffe depletion for every separated heterochiral event.

CQ, using current Wang true-upward support read-only, shows that every pair-creation event contains a genuine radial `donor -> high recipient` atom and that pure-UV true upward supply is first-shell only.

CS adds a repo-3 plateau transporter.  A genuinely deep donor/high-recipient edge is exactly mixed `V/h` work and therefore routes, after the same-event skew quotient, to resolved strain/deformation.  If it is not deep, the donor/high-recipient scale ratio is bounded.

Thus the remaining non-resolved critical action is local/bounded-ratio rather than arbitrarily nonlocal.

## 3. Sparse rooted cascades are not the critical mechanism

CK shows why topology alone is insufficient: the Hadamard lattice admits a formally Zeno-compatible heterochiral diamond.

The full Galerkin failure then exposed CN: the orthogonal equal-scale version is exactly 2D3C, births linearly polarized siblings, and has zero sibling self-composition.

Independently, CL/CM show that a fixed-critical rate gate requires effective Fourier participation of order the full spectral volume `N^3`.  Any fixed-width or sparse-triad ladder is asymptotically too weak even if several generations can be born.

The candidate obstruction is therefore a **dense** interaction state, not a shell-model genealogy.

## 4. The fixed-critical dense core has quantitative local rigidity

CT proves the universal heterochiral pair-capacity coefficient

\[
\eta_{pair}\le4\sqrt2/27.
\]

Under the natural `O(N^3)` support-volume bound and fixed participating critical mass, CR turns the record-rate lower into a positive action sublaw of uniformly work-efficient edges.

CO/CP prove that every nondegenerate fresh heterochiral edge generates both child helicities, with purity possible only as the Waleffe triangle degenerates.  Thus the efficient sublaw is also edgewise mixed-polarization.

CQ additionally binds first-shell pair action to the already-physical positive opposite-helicity child-work law, so no new causal probability is needed.

## 5. Late critical state is genuinely recent, but atomwise mixedness does not automatically descend to state mixedness

CU gives the exact high-pass mild identity and heat-memory erosion:

\[
\|G_R[t-L_N,t]\|_2
\ge\frac34\sqrt{\eta/N}
\]

for a late fixed-critical shell after the logarithmic natural-scale window.  The high-frequency vector therefore has a definite recent nonlinear source component.

However CV gives an exact rotational no-go.  After aligning the positive `+` child source, rotation of a parent plane by `phi` multiplies the minority source by `e^{2i phi}`.  Two efficient atoms at `phi=0,pi/2` can add in positive work and cancel their minority source exactly.

Therefore the invalid shortcut

\[
\text{edgewise mixed fresh source}
\Rightarrow
\text{mixed child state}
\]

is closed as a no-go.

The missing observable is the spin-2 azimuthal organization of the actual source law around each child axis, together with its temporal ancestry.

## 6. The one remaining theorem shape

A full proof mechanism would now need a theorem of the following literal form.

On every sufficiently late record-critical window carrying the positive net CF action, restrict to the actual first-shell comparable heterochiral work after all resolved/deep and terminal losses have been routed.  Then prove one of:

1. **source survives coherently:** a fixed fraction of the recent Duhamel amplitude inherits the efficient physical sublaw and produces a state consequence that cannot recur indefinitely under viscosity/material ancestry; or
2. **spin-2 / temporal cancellation is large:** the cancellation itself creates a quantitatively reusable phase/geometry/ancestry action which is forced into an already finite/nonaccumulating physical ledger; or
3. **fresh/reuse boundary:** work-weighted ancestry must become fresh-rich or reuse/cycle-rich with a quantitative charge that the existing Wang phase-holonomy/flat-rigidity machinery can consume without changing the physical work measure.

The theorem must be stated on the actual positive child-work law (or a proved bounded disintegration of it), not on analyst-chosen packet counts, pair capacity, or a new normalized pair-action probability.

## 7. What is no longer missing

The following are no longer legitimate names for the frontier:

- define an arbitrary `bad_flag`;
- find a global magic norm/currency;
- control generic covariance;
- control generic phase separately from work;
- invent FIFO/LIFO modal ancestry;
- count high-strain or HH events;
- prove every structural parent has fixed critical mass;
- control a sparse triad cascade;
- prove a single-triad Waleffe small factor;
- turn edgewise polarization into child-state polarization by absolute values.

All of those routes have either exact replacements or explicit no-go theorems in the current spine.

## 8. Current proof skeleton if the final attachment theorem closes

The main proof could then be compressed to:

\[
\boxed{
\text{first singular time}
\Rightarrow
\text{infinite positive net critical radial action}
\Rightarrow
\text{late first-shell dense heterochiral source}
\Rightarrow
\text{survival/cancellation/ancestry alternative}
\Rightarrow
\text{finite or nonaccumulating physical action}
\Rightarrow
\bot.}
\]

The discovery spine remains necessary for verification and no-go protection, but most of it would not appear in the final main argument.

**No global-regularity theorem is claimed at this stage.**
