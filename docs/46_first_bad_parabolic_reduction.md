# A literal own-scale continuation theorem would force the first-bad state onto the parabolic lower face

Status: **RIGOROUS CONDITIONAL FIRST-BAD REDUCTION / OPEN BRIDGE**.  Current Wang own-scale service is carrier-local and does not by itself supply the full-solution continuation hypothesis below.

The new killed-lineage mechanism shows exactly what a first-bad theorem must deliver.  This note isolates the missing implication without pretending it is already proved.

## 1. Candidate first singular time

Let `T` be a first candidate singular time for a smooth Navier--Stokes solution on `[0,T)`.  Suppose a literal physical bad/continuation event at time `t<T` and scale `N` has the following **full-PDE local continuation property**:

> if none of its already named physical exit faces fires, then the full smooth NSE solution extends from `t` for at least
> \[
> L_N=\frac{c_*}{\nu N^2},
> \qquad c_*>0.
> \]

This is a hypothesis about the actual solution, not about persistence of one smooth role or packet.

## 2. First-singular-time semantics force a lower parabolic face

If the event is a genuine continuing first-bad state and no exit fires, one cannot have

\[
L_N>T-t,
\]

because the local theorem would extend the full smooth solution beyond the alleged first singular time `T`.  Hence necessarily

\[
\frac{c_*}{\nu N^2}\le T-t.
\]

Therefore

\[
\boxed{
2\nu N^2(T-t)\ge2c_*.
}
\]

Thus a true own-scale continuation theorem would produce the parabolic lower face

\[
\boxed{\alpha=2c_*}
\]

from first-bad semantics itself.  No parabolic threshold has to be guessed.

**Classification: RIGOROUS CONDITIONAL CONSEQUENCE.**

If the programme uses a lifetime written as `c N^-2` with viscosity already absorbed into the nondimensional constant, the same statement is `alpha=2nu c`; the physical content is the dimensionless product `2nu N^2(T-t)`.

## 3. Current upstream status does not yet satisfy the hypothesis

Current Wang `critical_shell_service_reentry` proves that a critical shell either hits named strain/interface/HH coefficient faces or supports a smooth carrier/service statement over a natural `N^-2` corridor.  It explicitly remains shell/carrier-local and does not claim full NSE continuation beyond a candidate singular time.

Current Kelvin `dc26c0c` proves exact selected-current/deformation pair typing, including frozen-selector closure and mandatory selector--deformation cross terms under replica-dependent reselection.  It explicitly leaves the full finite current/state lift open-literal.

Therefore neither upstream is silently promoted to the full-PDE continuation property above.

**Classification: OPEN BRIDGE.**

## 4. Conditional proof reduction if the literal first-bad theorem is found

Suppose a future PDE theorem supplies, for every genuine continuing bad event:

1. the full-solution own-scale lifespan above, hence `a>=alpha=2c_*`;
2. a same-selected-energy critical floor `NE>=eta`;
3. a physical scale-locality upper ratio for continuing transitions, while nonlocal UV transfers are typed exits;
4. reverse/nonforward work is a typed exit rather than internal progress;
5. no free cloning/re-entry of selected mass after an exit.

Then:

- Theorem AY forces every low-to-high transition through a matched corridor;
- Theorem AX excludes scale-critical event accumulation inside any corridor with lower face `alpha>1/2`;
- Theorems AU--AV give a stronger explicit finite-depth bound when forward jump geometry also satisfies `c_jump>log Lambda`;
- phase/interface/source/reset/reselection failures remain named exits in the hybrid ledger rather than hidden escape.

This is now a short conditional proof skeleton.  The unresolved theorem is exactly the literal NSE first-bad/lifespan/energy-floor statement and the global handling of repeated typed exits/re-entry.

No regularity conclusion is claimed until those hypotheses are derived from NSE itself.
