# Repo-3 projector jumps cover pure selector changes, not Kelvin physical residual synthesis/refinement

Status: **EXACT SCOPE BRIDGE / NO-GO AGAINST COLLAPSING PHYSICAL REFINEMENT TO A PROJECTOR RESET**.

Current Kelvin `selected_principal_kelvin_lineage_audit.md` supplies the exact pair-state reset law for a physical residual synthesis map.  This sharpens the scope of repo-3 Theorem DR.

## 1. Pure observer/role reset with frozen physical state

Repo 3 proves that if the physical Hilbert-space state `y` is continuous and only an analysis projector changes,

\[
P^-\to P^+,
\]

then

\[
\boxed{
\Delta E_P
=\frac12\langle y,(P^+-P^-)y\rangle.
}
\]

This is the correct finite face for a **pure selector/relabeling change on a frozen physical state**.

## 2. Kelvin physical synthesis changes the state readout itself

Current Kelvin keeps a full germ/fiber residual library with second moment `mathbb Q` and a physical linear synthesis map `A`.  The synthesized residual second moment is

\[
\boxed{Q_A=A\mathbb Q A^T.}
\]

Under a physical synthesis/refinement change

\[
A_+=A_-+\Delta A,
\]

the state tensor changes by

\[
\boxed{
\Delta Q
=\Delta A\,\mathbb Q A_-^T
+A_-\mathbb Q\,\Delta A^T
+\Delta A\,\mathbb Q\,\Delta A^T.
}
\]

These are literal left, right, and quadratic pair faces forced by the tensor-square functor `A tensor A`.

## 3. Weighted physical event has geometry plus full pair reset

If the endpoint physical line metric also changes, Kelvin's exact weighted jump is

\[
\boxed{
\Delta\mathcal E
=\operatorname{tr}(\bar Q\,\Delta M)
+\operatorname{tr}(\bar M\,\Delta Q).
}
\]

Therefore the physical event contains:

1. geometry revaluation;
2. pair-left reset;
3. pair-right reset;
4. pair-quadratic reset.

A single projector jump cannot represent these four faces unless the physical synthesis is actually frozen.

## 4. Exact no-go from Kelvin's one-mode reset

Current Kelvin gives an exact periodic Navier--Stokes pair for which a selector/germ reset has equal endpoint physical weighted energies,

\[
\Delta\mathcal E=0,
\]

but the individual pair reset faces satisfy

\[
\mathcal R_L<0,
\qquad
\mathcal R_R<0,
\qquad
\mathcal R_Q>0,
\]

with

\[
\boxed{\mathcal R_L+\mathcal R_R+\mathcal R_Q=0.}
\]

Hence the positive quadratic term is not a reset cost, and replacing the full physical refinement by one positive projector-distance payment is false.

## 5. Acceptance rule for the Wang/Kelvin state-map bridge

A future map from Wang roles/coherent ancestry to Kelvin current/germ residual state must first decide which of two situations holds:

### Pure role/selector gauge

The physical state/library is fixed and only a projector changes.  Repo-3 DQ/DR applies directly.

### Physical synthesis/refinement

The residual observable itself changes through `A`.  The bridge must carry the full pair functor

\[
A\mapsto A\otimes A
\]

and all signed reset faces above.

No silent replacement of the second case by the first is permitted.

This is the current exact boundary of the selector/state-map bridge.
