# Wang cyclic hard-cell single charge is phase-diagonal provenance

Status: **Exact compatibility consequence.**

Read-only audit target: Wang upstream `8d21df4d1971f96c90fd0406136f4fa1882d3ad9`.
Its certified hard-cell theorem pushes the physical closed-triad donor/recipient
measure through deterministic hard Fourier/helicity cells, preserving canonical
negative donor work and canonical positive recipient work exactly once.

## 1. One triad, one complex phase

For one real closed helical triad, the third-repo common-phase theorem gives

\[
\mathcal Z_0=\mathcal Z_1=\mathcal Z_2=:\mathcal Z_\triangle,
\qquad
T_i=\kappa_i\operatorname{Re}\mathcal Z_\triangle,
\qquad
\sum_i\kappa_i=0.
\]

Thus donor and recipient roots differ by real energy-owner coefficients, not by
interaction phase.

**Classification: EXACT NSE/MATERIAL 3-FORM IDENTITY.**

## 2. The donor kernel is diagonal on the phase fiber

Attach the measurable mark

\[
\vartheta_\triangle=\arg\mathcal Z_\triangle
\]

to Wang's physical closed-triad donor/recipient measure whenever
`Z_triangle != 0`.  Every donor/recipient atom belongs to the same underlying triad,
so

\[
\boxed{\vartheta_{donor}=\vartheta_{recipient}.}
\]

After the deterministic hard-cell/fate pushforward, retaining this mark gives a
phase-marked hard table whose phase transition is still the identity.  Forgetting
the mark recovers Wang's current unmarked table.

**Classification: EXACT MEASURE-PUSHFORWARD CONSEQUENCE.**

Cyclic routing changes same-time energy ownership/provenance; it creates no phase
rotation.

## 3. Coarse self-loops are not phase loops

Current Wang explicitly retains hard-cell self-loops when distinct physical donor
and recipient roots collapse to the same coarse analyst cell.  Since their
underlying common phase is unchanged,

\[
\boxed{K(C,C,F)>0\not\Rightarrow\text{phase recurrence, phase rotation, or scale progress}.}
\]

The self-loop is label aliasing of real same-time NS work.  This matches Wang's
independent conclusions: zero additional event time, zero recursive depth, no
supplied scale progress.

**Classification: COUNTEREXAMPLE/NO-GO against phase/recurrence reading of a hard self-loop.**

## 4. The unmarked donor table cannot reconstruct phase

Choose

\[
\mathcal Z_+=Re^{i\theta},
\qquad
\mathcal Z_-=Re^{-i\theta},
\qquad 0<\theta<\pi/2.
\]

They have identical real parts.  Hence every root work, Hahn donor/recipient mass,
and hard-cell donor table is identical, while their phases are opposite:

\[
\boxed{K\not\Rightarrow\arg\mathcal Z_\triangle.}
\]

**Classification: COUNTEREXAMPLE/NO-GO.**

The single-charge theorem is an energy-routing theorem, not a hidden phase law.

## 5. Sign reversal

Negating a real divergence-free field sends the cubic interaction to
`-Z_triangle`, hence reverses all root works and swaps donor/recipient roles.  This
is the physical sign-reversal pattern audited by current Wang.  Within either event,
all roots still share one phase mark and routing remains phase-diagonal.

**Classification: EXACT CUBIC SIGN CONSEQUENCE.**

## 6. Hybrid-ledger placement

Wang hard-cell routing is a same-time provenance operation.  The third-repo hybrid
phase/work ledger instead accounts for continuous PDE phase/amplitude owners and
finite physical re-registration events.  Cyclic donor routing itself contributes
no phase jump.  A later nonlinear event may have a different phase because the
Navier--Stokes field evolved, not because the earlier donor table transported phase.

No event-to-event phase persistence, recurrence termination, or regularity theorem
is inferred.
