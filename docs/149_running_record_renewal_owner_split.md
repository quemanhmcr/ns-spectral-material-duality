# Running-record renewal gauge: local core, geometry harvesting, and domain-exit alternatives

Status: **EXACT NSE/PDE IDENTITY + RIGOROUS CONSEQUENCE + COUNTEREXAMPLE/NO-GO + ACTION STRESS TEST + OPEN BRIDGE**.

JA--JI showed that on one unique nondegenerate maximizing lineage the instantaneous-max normalization produces an exact Riccati law for scale-free stretching efficiency. This batch removes one remaining representation ambiguity: a temporary decrease of the current maximum `M(t)` must not look like positive renewal merely because the normalization denominator shrank.

Let
\[
M(t)=\max_x e(x,t),\qquad
\mathscr M(t)=\max_{s\in[t_0,t]}M(s).
\]

`mathscr M` is monotone and generated by the PDE field itself. The resulting grammar separates **local/material-rate renewal**, **critical-lineage geometry harvesting**, and **support-edge theorem-domain exit** without allowing record normalization to masquerade as a positive source.

Current read-only upstream heads remain Kelvin `5067f87b5a921d3f260433ec4f3ee1ce0df81f2c` and Wang `63178b0e7f9fabdfd8c344dab938a3d639639df5`. No upstream write is made.

---

## 1. The running record has an exact intrinsic contact law

For smooth periodic NSE, `M(t)` is locally Lipschitz and the running maximum `mathscr M` is absolutely continuous. At almost every time,
\[
\boxed{\dot{\mathscr M}=\mathbf 1_{\{M=\mathscr M\}}[\dot M]_+.}
\]

Assume the current maximum is represented by a differentiable active branch and define
\[
\mathcal D_\nu=\nu\bigl(|\nabla\omega|^2-\Delta e\bigr)\ge0
\]
at that maximum. Introduce
\[
\boxed{d\vartheta=\sqrt{\mathscr M}\,dt,\qquad
\Sigma=\frac{\alpha}{\sqrt{\mathscr M}},\qquad
\alpha=\xi^TS\xi.}
\]
Set
\[
\Delta_{\rm rec}=\frac{\mathcal D_\nu}{\mathscr M^{3/2}},\qquad
\rho_{\rm rec}=\frac{\dot{\mathscr M}}{\mathscr M^{3/2}}.
\]
At record contact `M=mathscr M`, `P=2M alpha` gives
\[
\boxed{\rho_{\rm rec}=\mathbf 1_{\{M=\mathscr M\}}[2\Sigma-\Delta_{\rm rec}]_+.}
\]
Thus the running record only moves when the current support edge reaches the historical edge and has positive physical owner margin.

**Label: EXACT NSE/PDE IDENTITY + RIGOROUS CONSEQUENCE.**

---

## 2. Running-record stretching efficiency obeys an exact Riccati law through subrecord excursions

On a unique nondegenerate current maximizing lineage `x_*(t)`, let
\[
w_*=\dot x_*-u=-H_e^{-1}\nabla R.
\]
The exact specific-stretching law from JD gives, after dividing by `mathscr M` and differentiating `Sigma=alpha/sqrt(mathscr M)` in `vartheta`,
\[
\boxed{\frac{d\Sigma}{d\vartheta}=-\Sigma^2+\mathfrak N_{\rm rec}.}
\]
This remains valid when the current maximum lies below the historical record. During a subrecord excursion `dot mathscr M=0`; there is no artificial increase of `Sigma` from a shrinking current maximum.

**Label: EXACT NSE/PDE IDENTITY.**

---

## 3. The running-record renewal excess has three exact causal faces

Write
\[
\boxed{\mathfrak N_{\rm rec}=\mathfrak N_{\rm core}+\mathfrak N_{\rm sweep}+\mathfrak N_{\rm norm}.}
\]
The **local/material-rate core** is
\[
\boxed{\begin{aligned}
\mathfrak N_{\rm core}={}&
\frac{|(I-\xi\xi^T)S\xi|^2}{\mathscr M}
-\frac{\xi^T(\nabla^2p)\xi}{\mathscr M}\\
&+\frac{\nu\,\xi^T(\Delta S)\xi}{\mathscr M}
+\frac{2\nu}{\mathscr M|\omega|}\Delta\omega\cdot(S\xi-\alpha\xi).
\end{aligned}}
\]
The **geometry-harvesting face** is
\[
\boxed{\mathfrak N_{\rm sweep}=\frac{w_*\cdot\nabla\alpha}{\mathscr M}.}
\]
The **record-normalization face** is
\[
\boxed{\mathfrak N_{\rm norm}=-\frac12\Sigma\rho_{\rm rec}.}
\]
These are distinct causal currencies. `N_core` changes the material specific-stretching rate; `N_sweep` changes what stretching environment the nonmaterial critical lineage samples; `N_norm` is only the kinematic cost of measuring efficiency relative to a growing historical record.

**Label: EXACT NSE/PDE IDENTITY.**

---

## 4. Running-record normalization can never be a positive renewal source

Because `rho_rec>=0`, if `Sigma>=0` then `N_norm<=0`. If `Sigma<0`, then at record contact
\[
2\Sigma-\Delta_{\rm rec}<0
\]
because `Delta_rec>=0`, hence `rho_rec=0`. Away from record contact `rho_rec=0` by definition. Therefore
\[
\boxed{\mathfrak N_{\rm norm}\le0.}
\]
A decline of the current maximum below the old record cannot mint positive scale-free renewal. This is the causal advantage of the running-record gauge over instantaneous-max normalization.

**Label: RIGOROUS CONSEQUENCE + COUNTEREXAMPLE/NO-GO to denominator-shrinkage renewal.**

---

## 5. Geometry harvesting is an exact inverse-Morse-metric pairing

At a strict nondegenerate maximum put
\[
G=-H_e>0.
\]
The critical-current law gives
\[
w_*=G^{-1}\nabla R.
\]
Normalize by the running record:
\[
\widehat G=\frac{G}{\mathscr M^{3/2}},\qquad
\widehat g_\alpha=\frac{\nabla\alpha}{\mathscr M^{3/4}},\qquad
\widehat g_R=\frac{\nabla R}{\mathscr M^{7/4}}.
\]
Then
\[
\boxed{\frac{w_*}{\mathscr M^{1/4}}=\widehat G^{-1}\widehat g_R,}
\]
and
\[
\boxed{\mathfrak N_{\rm sweep}=\widehat g_\alpha^T\widehat G^{-1}\widehat g_R.}
\]
Hence
\[
\boxed{|\mathfrak N_{\rm sweep}|\le
(\widehat g_\alpha^T\widehat G^{-1}\widehat g_\alpha)^{1/2}
(\widehat g_R^T\widehat G^{-1}\widehat g_R)^{1/2}.}
\]
Geometry harvesting therefore requires both a nonflat specific-stretching landscape and a nonzero critical relative drift in the inverse curvature metric. Critical speed alone is not enough.

**Label: EXACT NSE/PDE IDENTITY + RIGOROUS CONSEQUENCE.**

---

## 6. The transverse core face is literal vorticity-direction turning

The exact unit-vorticity equation is
\[
D_t\xi=(I-\xi\xi^T)\left(S\xi+\nu\frac{\Delta\omega}{|\omega|}\right).
\]
Therefore
\[
\boxed{(I-\xi\xi^T)S\xi=D_t\xi-\nu(I-\xi\xi^T)\frac{\Delta\omega}{|\omega|}.}
\]
The nonnegative transverse part of `N_core` is consequently
\[
\boxed{
\frac{|(I-\xi\xi^T)S\xi|^2}{\mathscr M}
=\frac{\left|D_t\xi-\nu(I-\xi\xi^T)\Delta\omega/|\omega|\right|^2}{\mathscr M}.}
\]
For Euler (`nu=0`) it is exactly the squared material turning rate of the vorticity direction, normalized by the running enstrophy record. It is a physical orientation-conversion face, not an abstract norm inserted from outside.

**Label: EXACT NSE/PDE IDENTITY.**

---

## 7. Nonpositive running-record renewal excludes finite-time record divergence even across dips

Assume that from some `vartheta_0` onward the current maximizer remains one differentiable nondegenerate branch and
\[
\mathfrak N_{\rm rec}\le0.
\]
Then
\[
\frac{d\Sigma}{d\vartheta}\le-\Sigma^2.
\]
If `Sigma_0<=0`, the running-record contact law gives `rho_rec=0` and the record cannot increase. If `Sigma_0>0`,
\[
\Sigma(\vartheta)\le\frac{\Sigma_0}{1+\Sigma_0(\vartheta-\vartheta_0)}.
\]
Since `rho_rec<=2[Sigma]_+`,
\[
\boxed{\mathscr M(\vartheta)\le\mathscr M_0[1+\Sigma_0(\vartheta-\vartheta_0)]^2.}
\]
Finally `dt/dvartheta=mathscr M^{-1/2}`, so infinite `vartheta` requires infinite physical time under this quadratic bound. Finite `vartheta` leaves `mathscr M` finite. Hence eventual nonpositive `N_rec` on a persistent branch excludes finite-physical-time divergence of the running record.

**Label: RIGOROUS CONSEQUENCE.**

---

## 8. A hypothetical finite-time unbounded record has a three-channel intrinsic alternative

Suppose `mathscr M(t)` becomes unbounded as `t` approaches a finite endpoint. If the unique nondegenerate maximizing-lineage theorem domain does not persist arbitrarily late, the solution exits through the support-edge geometry/lineage channel.

Otherwise Section 7 forces `N_rec>0` arbitrarily late. Section 4 gives `N_norm<=0`, so at those renewal times
\[
\mathfrak N_{\rm core}+\mathfrak N_{\rm sweep}>0.
\]
Consequently at least one of the following must occur arbitrarily late:
\[
\boxed{\mathfrak N_{\rm core}>0
\quad\text{or}\quad
\mathfrak N_{\rm sweep}>0
\quad\text{or}\quad
\text{support-edge theorem-domain exit}.}
\]
This is an intrinsic three-channel reduction. It does not prove any channel finite or impossible.

**Label: RIGOROUS CONSEQUENCE + OPEN BRIDGE.**

---

## 9. Exact periodic ABC calibration

Use the viscously decaying unit ABC Beltrami solution with amplitude `a=exp(-nu t)` and the fixed strict critical point `x_*=(pi/4,pi/4,pi/4)`. At that point `U_0=(sqrt(2),sqrt(2),sqrt(2))`, `S=0`, and `alpha=0`.

The unit-amplitude enstrophy Hessian is `-(I+11^T)/2` and the pressure Hessian is `(I+11^T)/2`. Anchor the running record at `t_0=0`; then `mathscr M=3` during the subsequent viscous decay.

The local material-rate core is pure pressure suppression,
\[
\mathfrak N_{\rm core}=-\frac{2a^2}{3}.
\]
The critical point is fixed while the fluid velocity is nonzero, so its relative critical velocity equals minus the fluid velocity. Exact differentiation of `alpha` gives
\[
\mathfrak N_{\rm sweep}=+\frac{2a^2}{3}.
\]
Since `Sigma=0` and the running record is flat,
\[
\mathfrak N_{\rm norm}=0,\qquad \mathfrak N_{\rm rec}=0.
\]
Thus positive geometry harvesting can exactly cancel suppressive local material renewal without changing selected stretching efficiency. Positive sweep is not by itself fresh local generation.

**Label: EXACT NSE/PDE IDENTITY + ACTION STRESS TEST calibration.**

---

## 10. Singular critical speed can have exactly zero geometry harvesting

For every exact x-shear `u=(U(y,t),0,0)`, vorticity is parallel to `e_z` while `S` has only the `xy` symmetric block. Hence wherever `omega!=0`,
\[
S\xi=0,\qquad\alpha=0,\qquad\nabla\alpha=0.
\]
Therefore `N_sweep=0` on every critical sheet, regardless of its relative speed. In the exact two-mode merger, the critical-sheet speed diverges like `1/d`, yet the geometry-harvesting face remains exactly zero.

A fast or even divergent critical/readout motion cannot fund renewal without coupling to a nonflat specific-stretching landscape.

**Label: EXACT NSE/PDE IDENTITY + COUNTEREXAMPLE/NO-GO.**

---

## 11. Cross-program recurrence admission is now a three-channel question

The current exact grammar does not license calling all positive `N_rec` a new recursive owner. `N_core` is a local material-rate face that still needs strain/pressure/viscous owner resolution. `N_sweep` is geometry harvesting through an existing stretching landscape and remains transport rather than generation absent independent evidence. Support-edge theorem-domain exit is a geometry event needing merger/degeneracy/ancestry resolution. `N_norm` is never a positive source in the running-record gauge.

This matches current Wang's fail-closed native-service rule and current Kelvin's moving-readout rule. The remaining hard seam is to prove a finite-capacity/reuse law for positive `N_core`, a finite admissible harvesting law for `N_sweep`, or nonaccumulation of support-edge theorem-domain exits under the full Kelvin/Nanson/Wang state.

No termination theorem is claimed here.

**Label: RIGOROUS CROSS-PROGRAM CONSEQUENCE + OPEN BRIDGE.**

---

## 12. Action certification

GitHub Actions run `31709932079` on theorem SHA `83ee7509e638369f104ced57d3c1c4164915731a` completed with conclusion **success**.

Selected exp99 metrics:
- constant-affine split residual: `0`;
- constant-affine largest normalization face: `-3.375041e-02`;
- accelerating-affine split residual: `2.233e-16`;
- accelerating-affine minimum positive core face: `1.983733e+00`;
- accelerating-affine largest normalization face: `-9.918667e-01`;
- ABC tensor/pressure/alpha-gradient residual: `1.665e-16`;
- ABC core-sweep cancellation residual: `2.220e-16`;
- ABC inverse-Morse sweep residual: `1.258e-16`;
- ABC minimum positive sweep signal: `2.365809e-01`;
- ABC minimum Cauchy bound gap: `-5.551e-17`;
- singular heat-shear critical-speed signal: `1.110000e+07`;
- x-shear alpha / harvesting residual: `0 / 0`;
- PASS.

**Label: ACTION STRESS TEST.**
