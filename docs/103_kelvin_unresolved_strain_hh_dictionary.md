# Kelvin unresolved strain and Wang HH work share `S_h`, but not a scalar charge

Status: **EXACT CROSS-PROGRAMME TENSOR DICTIONARY / NO-GO AGAINST HH--KELVIN CHARGE IDENTIFICATION**.

## 1. The tensor shared by the two programmes

For the physical split

\[
u=V+h,
\]

current Kelvin residual dynamics contain the deterministic unresolved correction

\[
-r\cdot S_hr,
\qquad S_h=\operatorname{sym}\nabla h.
\]

Theorem DG shows that the **same tensor `S_h`** is the symmetric Cartan component of Wang's actual HH nonlinear role ledger.

Thus

\[
\boxed{
\text{Kelvin unresolved strain probe}
\quad\text{and}\quad
\text{Wang HH symmetric role work}
\text{ evaluate the same }S_h.
}
\]

## 2. Their probes are physically different

Kelvin evaluates `S_h` on current-shape vectors such as

\[
\mathfrak s_h(r,r)=r\cdot S_hr,
\qquad
\mathfrak s_h(\omega,r)=\omega\cdot S_hr.
\]

Wang evaluates it on spectral/event roles,

\[
S^{(h)}_{ab}=\int w_a\cdot S_hw_b\,dx.
\]

The actual HH child work also contains the unresolved skew row:

\[
T_a^{HH}
=\frac12\sum_bK^{(h)}_{ab}
+\frac12\sum_bS^{(h)}_{ab}.
\]

Therefore Kelvin unresolved strain, Wang symmetric HH work, and Wang canonical positive HH transfer are three **typed readings** of overlapping PDE structure, not interchangeable scalar budgets.

## 3. The connection sector is exactly what prevents the collapse

`K^(h)` is antisymmetric as a role-space 2-form, so it disappears from total quadratic energy but can move energy between simultaneous roles.  Kelvin symmetric residual-energy/cross-dyad observables cancel the skew connection for a different reason: transpose-dual quadratic pairing.

The same skew physics is therefore invisible in both totals, but it remains essential to the rolewise HH transfer law and to orientation/phase information.

## 4. Consequence for future bridge work

A theorem that wants to pass from Wang HH work to Kelvin current-shape information must carry at least:

1. the canonical HH donor/recipient cause;
2. the unresolved symmetric tensor `S_h`;
3. the unresolved connection/relink 2-form `K_h` or an exact quotient of it;
4. the Kelvin probe/current-shape state;
5. q.v./cross-q.v. when the Kelvin observable is stochastic.

Dropping any of these by calling them all “high-frequency activity” loses a literal PDE face.

No recurrence or regularity statement is made.
