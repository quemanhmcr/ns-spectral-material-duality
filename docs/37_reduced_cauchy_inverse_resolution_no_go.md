# Reduced-state Cauchy inverse formulas require explicit covariance and top-exterior resolution faces

Status: **Exact law-of-total-covariance/exterior-resolution identity plus exact affine Navier--Stokes no-go calibrations.**

This note re-audits current Kelvin upstream `ceca144d51b8585986145f89323fbffa6f075d6e`.
That commit now explicitly places the full vectorized Cauchy covariance in the
existing connected pair/covariance ledger and proves, for a reduced/full lift kernel
`R(y,dY)`,

\[
\Sigma_D^{red}
=R\Sigma_D+\operatorname{Cov}_R(\bar z),
\qquad
\bar z=\operatorname{vec}\bar D.
\]

The second term is additional hidden-state resolution covariance, not intrinsic
same-clock deformation covariance.  The third-repo inverse dictionary must therefore
be typed through this reduction face before being used on reduced ancestry data.

---

## 1. Both Gram projections inherit separate resolution faces

Let

\[
C_{row}(Y)=E[DD^T\mid Y]-\bar D(Y)\bar D(Y)^T,
\]

\[
C_{col}(Y)=E[D^TD\mid Y]-\bar D(Y)^T\bar D(Y).
\]

Taking the two partial traces of the exact vector law of total covariance gives

\[
\boxed{
C_{row}^{red}
=R C_{row}+C_{row}^{res},
}
\]

\[
\boxed{
C_{col}^{red}
=R C_{col}+C_{col}^{res},
}
\]

where

\[
C_{row}^{res}
=R[\bar D\bar D^T]-(R\bar D)(R\bar D)^T,
\]

\[
C_{col}^{res}
=R[\bar D^T\bar D]-(R\bar D)^T(R\bar D).
\]

**Classification: EXACT CONDITIONAL-COVARIANCE CONTRACTION IDENTITY.**

Thus the dual row/column geometry survives reduction, but each projection acquires
its own hidden-state Gram-resolution face.

---

## 2. Mean top exterior power has an independent resolution face

Define at full state

\[
\delta(Y)=1-\det\bar D(Y),
\]

and at reduced state

\[
\delta^{red}=1-\det(R\bar D).
\]

Since `R1=1`,

\[
\boxed{
\delta^{red}
=R\delta+\delta_{\Lambda^3}^{res},
}
\]

with

\[
\boxed{
\delta_{\Lambda^3}^{res}
:=R[\det\bar D]-\det(R\bar D).
}
\]

**Classification: EXACT TOP-EXTERIOR RESOLUTION IDENTITY.**

This is not determined by the second-order resolution covariance in general.  It is
the same failure of averaging to commute with `Lambda^3` already seen in the
same-clock replica problem, now occurring across hidden full physical states.

---

## 3. Reduced inverse formulas contain explicit resolution contamination

Let

\[
T^{red}_h=\operatorname{tr}C_{row}^{red},
\qquad
T^{res}_h=\operatorname{tr}C_{row}^{res}.
\]

Then exactly

\[
T^{red}_h=R T_h+T_h^{res},
\]

and

\[
\delta_h^{red}=R\delta_h+\delta_{\Lambda^3,h}^{res}.
\]

Therefore the scalar combinations from Theorem AM become

\[
\boxed{
\frac{3T_h^{red}+6\delta_h^{red}}{h^3}
=R\frac{3T_h+6\delta_h}{h^3}
+
\frac{3T_h^{res}+6\delta_{\Lambda^3,h}^{res}}{h^3},
}
\]

\[
\boxed{
\frac{3T_h^{red}-6\delta_h^{red}}{4h^3}
=R\frac{3T_h-6\delta_h}{4h^3}
+
\frac{3T_h^{res}-6\delta_{\Lambda^3,h}^{res}}{4h^3}.
}
\]

**Classification: EXACT REDUCED INVERSE LEDGER.**

The first terms converge to averaged physical Kelvin-q.v./strain-gradient currencies
when the full-state smooth hypotheses hold.  The second terms are hidden-state
resolution and must be subtracted, proved negligible, or retained as separate
owners.  They cannot be silently read as PDE gradients.

---

## 4. Generic hidden deformation variation appears one order earlier

Suppose every full hidden state has

\[
\bar D_Y(h)=I+hA_Y^T+O(h^2),
\qquad
\operatorname{tr}A_Y=0.
\]

Then

\[
\bar D_Y-R\bar D
=h(A_Y^T-R A^T)+O(h^2),
\]

so generically

\[
\boxed{
C_{row}^{res}=O(h^2),
\qquad
C_{col}^{res}=O(h^2).
}
\]

The full intrinsic same-clock Cauchy covariance generated by Brownian anchor
sampling begins at `O(nu h^3)`.  Thus unresolved mixing of different current
velocity gradients can dominate the intrinsic Cauchy inverse scale by one power of
`h`.

**Classification: RIGOROUS SHORT-HORIZON CONSEQUENCE.**

This is a state-resolution effect, not stronger physical diffusion.

---

## 5. Exact affine strain pair: false strain-gradient signal from pure hidden-state mixing

Consider two exact smooth affine incompressible Navier--Stokes states

\[
A_+=S,
\qquad
A_-=-S,
\qquad
S=\operatorname{diag}(a,-a,0),
\]

with the appropriate quadratic pressures.  Each has spatially constant velocity
gradient, hence

\[
\nabla S=0,
\qquad
\nabla\omega=0,
\qquad
\Sigma_D=0
\]

inside each full state.  Their deterministic reverse-age Cauchy deformations are

\[
D_\pm=e^{\pm hS}.
\]

Let one reduced state forget which sign is present and assign equal weight `1/2` to
the two full states.  Writing `x=ah`,

\[
R\bar D
=\operatorname{diag}(\cosh x,\cosh x,1).
\]

The entire reduced covariance is resolution covariance and

\[
\boxed{
T_h^{red}=2\sinh^2x,
}
\]

while

\[
\boxed{
\delta_h^{red}
=1-\cosh^2x
=-\sinh^2x.
}
\]

Hence

\[
3T_h^{red}+6\delta_h^{red}=0,
\]

but

\[
\boxed{
\frac{3T_h^{red}-6\delta_h^{red}}{4h^3}
=\frac{3\sinh^2(ah)}{h^3}
\sim\frac{3a^2}{h}\to\infty.
}
\]

The true physical full-state value is

\[
\nu|\nabla S|_F^2=0.
\]

**Classification: EXACT AFFINE NSE COUNTEREXAMPLE/NO-GO.**

Naively applying the reduced AM strain inverse would manufacture an infinite
strain-gradient signal from nothing but hidden-state mixing.

---

## 6. Exact rigid-rotation pair: false Kelvin-q.v. signal from pure hidden-state mixing

Now take two exact rigid-rotation Navier--Stokes states

\[
A_+=\Omega,
\qquad
A_-=-\Omega,
\]

with

\[
\Omega=
\begin{pmatrix}
0&-a&0\\
a&0&0\\
0&0&0
\end{pmatrix}.
\]

Each is a smooth affine Euler/Navier--Stokes solution with centrifugal quadratic
pressure.  The velocity gradient and vorticity are spatially constant, so again

\[
\nabla S=0,
\qquad
\nabla\omega=0,
\qquad
\Sigma_D=0
\]

inside each full state.

The two deterministic Cauchy deformations are opposite planar rotations.  Under the
same equal-weight reduced kernel,

\[
R\bar D
=\operatorname{diag}(\cos x,\cos x,1),
\qquad x=ah.
\]

The reduced resolution trace is

\[
\boxed{T_h^{red}=2\sin^2x,}
\]

and

\[
\boxed{
\delta_h^{red}
=1-\cos^2x
=\sin^2x.
}
\]

Therefore

\[
3T_h^{red}-6\delta_h^{red}=0,
\]

but

\[
\boxed{
\frac{3T_h^{red}+6\delta_h^{red}}{h^3}
=\frac{12\sin^2(ah)}{h^3}
\sim\frac{12a^2}{h}\to\infty.
}
\]

The true physical full-state Kelvin q.v. trace is zero because
`grad omega=0`.

**Classification: EXACT AFFINE NSE COUNTEREXAMPLE/NO-GO.**

So hidden unresolved rotation can masquerade as arbitrarily large instantaneous
Kelvin q.v. if the resolution face is omitted.

---

## 7. Correct reduced-state inverse statement

The inverse dictionary is physically valid at reduced state only in one of the
following typed forms:

1. the lift kernel is Dirac on the relevant deformation state;
2. the resolution faces are known and subtracted exactly;
3. they are proved `o(h^3)` under a programme-specific state-sufficiency theorem;
4. the inverse statement explicitly returns **physical average + resolution owner**
   rather than renaming their sum.

**Classification: RIGOROUS ADMISSIBILITY CONSEQUENCE.**

Current Kelvin upstream `ceca144` supplies the exact covariance decomposition but
still leaves the actual programme-specific ancestry lift kernel open-literal.  This
third-repo theorem therefore narrows the state-alignment frontier; it does not close
it by assumption.
