# Exact heterochiral Waleffe geometry depletes every separated pair-creation event

Status: **EXACT NSE/WALEFFE GEOMETRY / RIGOROUS SEPARATED-SCALE CONSEQUENCE**.

## 1. Exact pair-action capacity formula

Use the heterochiral split normal form

\[
-b<a<c,
\qquad a,b,c>0,
\]

and let `Delta(a,b,c)` be the Euclidean triangle area.  Current Wang's exact closed-triad coupling magnitude is

\[
|g_\triangle|
=
\frac{\Delta(a,b,c)\,(a+c-b)}{2\sqrt2\,abc},
\]

because the heterochiral signed sum is `-b+a+c>0` by strict triangle inequality.  The common physical phase factor is

\[
R_\triangle
=4\Re(a_0a_1a_2\overline{g_\triangle}).
\]

The pair-creation charge is exactly

\[
\mathcal P_\triangle=b(c-a)|R_\triangle|.
\]

Therefore

\[
\boxed{
\mathcal P_\triangle
\le
\sqrt2\,\Delta(a,b,c)
\frac{(c-a)(a+c-b)}{ac}
|a_0a_1a_2|.
}
\]

This is the native helical modal-capacity envelope of the actual pair action.  It is not substituted for the causal work law.

## 2. Low donor: one low/high depletion factor

Assume the donor radius `a` is the low leg and let

\[
K=\max\{b,c\}.
\]

Triangle geometry gives

\[
|c-b|<a,
\qquad
0<a+c-b<2a.
\]

Also, using the two sides `a,b`,

\[
\Delta\le\frac12ab.
\]

Since `c-a<c`, the exact capacity formula yields

\[
\boxed{
\mathcal P_\triangle
\le
\sqrt2\,ab\,|a_0a_1a_2|
\le
\sqrt2\,aK\,|a_0a_1a_2|.
}
\]

Thus a low-donor/high-pair event loses at least one physical scale ratio `a/K` relative to an unsuppressed local `K^2|a_0a_1a_2|` critical action.

This is the spectral signature of the resolved low--high/parametric-strain geometry already identified by the owner calculus.

## 3. Low opposite-helicity recipient: two low-scale factors

If instead `b` is the low opposite-helicity recipient, then strict triangle geometry gives

\[
0<c-a<b.
\]

Again `Delta<=ab/2` and `a+c-b<2a`, hence

\[
\boxed{
\mathcal P_\triangle
\le
\sqrt2\,\frac{a}{c}\,b^2|a_0a_1a_2|
<
\sqrt2\,b^2|a_0a_1a_2|.
}
\]

This sharpens the earlier capacity-level `4b^2` envelope.  The opposite-helicity low-output branch is quadratically suppressed by its actual low physical radius.

## 4. No third separated geometry

Because `c>a`, the smallest physical side is either `a` or `b`.  Thus every genuinely separated heterochiral pair-creation event is one of the two cases above:

1. low donor / high pair -- one low/high factor;
2. low opposite recipient -- two low factors.

The only branch without a small scale ratio is the fully comparable branch.

**Classification: RIGOROUS CONSEQUENCE OF EXACT PHYSICAL TRIAD GEOMETRY.**
