# The two helical critical stocks have one common nonlinear source

Status: **EXACT NSE/HELICITY IDENTITY**.

## 1. Sector stocks

Define

\[
\mathcal C_+
=\sum_k |k|E_{k,+},
\qquad
\mathcal C_-
=\sum_k |k|E_{k,-},
\]

and their cubic viscous moments

\[
\mathcal B_+
=\sum_k |k|^3E_{k,+},
\qquad
\mathcal B_-
=\sum_k |k|^3E_{k,-}.
\]

Then

\[
\mathcal C=\mathcal C_++\mathcal C_-,
\qquad
\mathcal H=\mathcal C_+-\mathcal C_-
\]

is respectively absolute critical mass and signed helicity.

## 2. Add the exact absolute and signed ledgers

Theorem BY/CD gives

\[
(\mathcal C_++\mathcal C_-)'
+2\nu(\mathcal B_++\mathcal B_-)
=2\mathcal N,
\]

where

\[
\mathcal N
:=\mathcal P_{create}-\mathcal P_{ann}
=\frac12J_1.
\]

The ordinary unforced NSE helicity identity is

\[
(\mathcal C_+-\mathcal C_-)'
+2\nu(\mathcal B_+-\mathcal B_-)=0.
\]

Adding and subtracting yields the exact twin ledgers

\[
\boxed{
\mathcal C_+'+2\nu\mathcal B_+=\mathcal N,
}
\]

\[
\boxed{
\mathcal C_-'+2\nu\mathcal B_-=\mathcal N.
}
\]

The same nonlinear source is injected into both absolute-helicity sectors.

## 3. Physical meaning

- Homochiral split/merge traffic contributes zero to **each** critical sector after its own signed-frequency barycenter cancellation.
- A heterochiral split creates equal critical charge in the two sectors, so both receive `+P_create`.
- A heterochiral merge annihilates equal critical charge, so both receive `-P_ann`.
- Viscosity removes the two sector stocks at their own actual cubic spectral moments.

Thus the last positive global owner is literally a **paired two-sector injection**.  It cannot increase one helicity sector without increasing the other by the same critical amount at that event.

## 4. Exact polarization variables

The common-mode and difference-mode coordinates are

\[
\frac{\mathcal C_++\mathcal C_-}{2}
\quad\text{and}\quad
\frac{\mathcal C_+-\mathcal C_-}{2}.
\]

The nonlinear pair source acts only on the common mode.  The difference mode -- signed helicity -- is affected only by viscosity.  Therefore nonlinear blow-up production is a depolarizing pair mechanism, not independent growth of one helical sign.

This does not bound the common-mode source.  It identifies the exact two-sector state on which any final non-explosion theorem must act.
